// let num = 0;
// while (num < 10){ //Esto es la condicion, mientras que num sera menos que diez muestramelo.
//     console.log(num);
//     num++; //Esto suma 1 valor a la variable cada vez que reinicia el ciclo.
// }


let num = 0;
while (num <= 10){ //Esto es la condicion, mientras que num sera menos que diez muestramelo.
    console.log(num);
    num+=2; //Esto suma 1 valor a la variable cada vez que reinicia el ciclo.
}