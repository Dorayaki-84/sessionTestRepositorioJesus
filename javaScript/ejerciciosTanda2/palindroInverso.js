let frase = "No lemon, no melon"; //declaramos la variable
let letras = frase.split(""); //sacamos las letras una a una en un array.

console.log(letras);

letras.reverse(); //con reverse, damos la vuelta al array creado.
console.log(letras);


